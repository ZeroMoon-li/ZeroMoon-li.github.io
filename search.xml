<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>从零开始学习Java</title>
      <link href="/2022/03/19/%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AD%A6Java/"/>
      <url>/2022/03/19/%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AD%A6Java/</url>
      
        <content type="html"><![CDATA[<p>主要用于记录Java的一些基本知识以及在学习Java过程中所遇到的问题。</p><h2 id="类与对象"><a href="#类与对象" class="headerlink" title="类与对象"></a>类与对象</h2><p>类是Java程序的基本要素，一个Java应用程序就是由若干个类所构成的。类是Java语言中最重要的”数据类型”，类声明的变量被称为对象变量，简称对象。<br>   类的定义包括两部分：类声明和类体。基本格式为：</p><blockquote><p>class 类名 {<br>    类体的内容<br>  }</p></blockquote><h3 id="类声明"><a href="#类声明" class="headerlink" title="类声明"></a>类声明</h3><p>“class 类名 “ 被称为类声明。编程风格：如果类名使用拉丁字母时，那么名字的首字母要大写，如Hello、Hi等。当类名有几个单词复合而成时，每个单词的首字母应大写，如HelloWorld。</p><h3 id="类体"><a href="#类体" class="headerlink" title="类体"></a>类体</h3><p>类的目的是抽象出一类事物共有的属性和行为，并用一定的语法格式来描述所抽象出的属性和行为。类声明之后的一对大括号“{” “}”以及它们之间的内容称作类体，大括号之间的内容称作类体的内容。<br>类的内容由如下所述的两部分组成：</p><ul><li>变量的声明：用来储存属性的值（体现对象的属性）。  </li><li>方法的定义：方法可以对类中声明的变量进行操作，即给出算法（体现对象所具有的行为）。  </li></ul><p>下面是一个类名为Rectangle的类（用来描述长方形）。例如：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Rectangle</span>&#123;</span><br><span class="line">  <span class="type">float</span> length; <span class="comment">//长方形的长（变量声明）</span></span><br><span class="line">  <span class="type">float</span> width; <span class="comment">//长方形的宽（变量声明）</span></span><br><span class="line">  <span class="type">float</span> area; <span class="comment">//长方形的面积（变量声明）</span></span><br><span class="line">  <span class="type">float</span> <span class="title function_">computerArea</span><span class="params">()</span>&#123;    <span class="comment">//定义方法computerArea</span></span><br><span class="line">    area = length*width;</span><br><span class="line">    <span class="keyword">return</span> area;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">void</span> <span class="title function_">setData</span><span class="params">(<span class="type">float</span> L,<span class="type">float</span> W)</span>&#123;    <span class="comment">//定义方法setData</span></span><br><span class="line">    length = L;</span><br><span class="line">    width = W;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;   </span><br></pre></td></tr></table></figure><h4 id="成员变量"><a href="#成员变量" class="headerlink" title="成员变量"></a>成员变量</h4><ul><li>成员变量的类型可以是Java的任何一种数据类型。</li><li>成员变量在整个类内都有效，其有效性与它在类体中书写的先后位置无关，例如，前面的Rectangle类也可以等价地写成（不提倡这种写法）：</li></ul><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Rectangle</span>&#123;</span><br><span class="line">  <span class="type">float</span> length; <span class="comment">//长方形的长（变量声明）</span></span><br><span class="line">  <span class="type">float</span> width; <span class="comment">//长方形的宽（变量声明）</span></span><br><span class="line">  <span class="type">float</span> <span class="title function_">computerArea</span><span class="params">()</span>&#123;    <span class="comment">//定义方法computerArea</span></span><br><span class="line">    area = length*width;</span><br><span class="line">    <span class="keyword">return</span> area;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">void</span> <span class="title function_">setData</span><span class="params">(<span class="type">float</span> L,<span class="type">float</span> W)</span>&#123;    <span class="comment">//定义方法setData</span></span><br><span class="line">    length = L;</span><br><span class="line">    width = W;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="type">float</span> area; <span class="comment">//长方形的面积（变量声明）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="实例变量和类变量"><a href="#实例变量和类变量" class="headerlink" title="实例变量和类变量"></a>实例变量和类变量</h5><p>成员变量可细分为实例变量和类变量。在声明成员变量时，用关键字static给与修饰的称作类变量，否侧称作实例变量，（类变量也称作static变量、静态变量）例如</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span>&#123;</span><br><span class="line">  <span class="type">int</span> x;  <span class="comment">//实例变量</span></span><br><span class="line">  <span class="keyword">static</span> <span class="type">int</span> y;  <span class="comment">//类变量</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>两者的区别：</p><ul><li>不同对象的实例变量。使用new运算符创建多个不同对象时，分配给不同对象的实例变量占有不同的内存空间，改变其中一个对象的实例变量不会影响其它对象的实例变量。</li><li>所有对象共享类变量。使用new运算符创建多个不同对象时，分配给这些对象的这个类变量占有相同的一处内存，改变其中一个对象的这个类变量会影响其它对象的这个类变量，也就是说，对象共享类变量。</li><li>类变量可以通过类名直接访问。</li></ul><h4 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h4><p>方法的定义有包括两部分：方法头和方法体，一般格式为：</p><blockquote><p>方法头 {<br>      方法体的内容<br>  }  </p></blockquote><h5 id="方法头"><a href="#方法头" class="headerlink" title="方法头"></a>方法头</h5><p>方法头由方法的类型、名称和名称之后的一对小括号以及其中的参数列表所构成。可根据程序的需要，方法返回的数据类型可以是Java的任何一种数据类型，当一个方法是void类型时，该方法就不需要返回数据。例如：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> <span class="title function_">computerArea</span><span class="params">()</span> <span class="comment">//无参数的方法头</span></span><br><span class="line">&#123;    </span><br><span class="line">  area = length*width;</span><br><span class="line">  <span class="keyword">return</span> area;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">void</span> <span class="title function_">setData</span><span class="params">(<span class="type">float</span> L,<span class="type">float</span> W)</span>  <span class="comment">//有参数的方法头</span></span><br><span class="line">&#123;</span><br><span class="line">  length = L;</span><br><span class="line">  width = W;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="方法体"><a href="#方法体" class="headerlink" title="方法体"></a>方法体</h5><p>方法声明之后的一对大括号{、}以及它们之间的内容被称为方法的方法体。方法体的内容包括局部变量的声明和Java语句，即在方法体内可以对成员变量和方法体中声明的局部变量进行操作。在方法体中声明的变量和方法的参数被称作局部变量，例如(求n!的方法):</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">getFactorial</span><span class="params">(<span class="type">int</span> n)</span>&#123;  <span class="comment">//参数n是局部变量</span></span><br><span class="line">  <span class="type">int</span> <span class="variable">factorial</span> <span class="operator">=</span> <span class="number">1</span>; <span class="comment">//声明局部变量factorial</span></span><br><span class="line">  <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">    factorial = factorial*i;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> factorial;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h6 id="局部变量"><a href="#局部变量" class="headerlink" title="局部变量"></a>局部变量</h6><ul><li>和类的成员变量不同的是，局部变量只在方法内有效，而且与其声明的位置有关。方法的参数在整个方法内有效，方法内的局部变量从声明它的位置之后开始有效。如果局部变量的声明是在一个复合语句中，那么该局部变量的有效范围是该复合语句；如果局部变量的声明是在一个循环语句中，那么该局部变量的有效范围是该循环语句。例如：<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">A</span>&#123;</span><br><span class="line">  <span class="type">int</span> <span class="variable">n</span> <span class="operator">=</span> <span class="number">4</span>,factorial = <span class="number">1</span>; <span class="comment">//成员变量，在整个类有效</span></span><br><span class="line">  <span class="keyword">void</span> <span class="title function_">f</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(n&gt;<span class="number">3</span>) &#123;</span><br><span class="line">      <span class="type">int</span> <span class="variable">z</span> <span class="operator">=</span> <span class="number">2</span>; <span class="comment">//z仅仅在该复合语句中有效</span></span><br><span class="line">      z = z*n+z;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">      factorial = factorial*i;<span class="comment">//i仅仅在该循环语句中有效</span></span><br><span class="line">    &#125;</span><br><span class="line">    n = factorial; <span class="comment">//合法，因为n和factorial有效</span></span><br><span class="line">    z = i+factorial; <span class="comment">//非法，因为i和z已失效</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li>如果局部变量的名字与成员变量的名字相同，那么成员变量被隐藏，即该成员变量在这个方法暂时失效。如果想在该方法中使用被隐藏的成员变量，必须使用关键句this。例如：<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span>&#123;</span><br><span class="line">  <span class="type">int</span> <span class="variable">x</span> <span class="operator">=</span> <span class="number">10</span>,y,z;</span><br><span class="line">  <span class="keyword">void</span> <span class="title function_">f</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">x</span> <span class="operator">=</span> <span class="number">5</span>;</span><br><span class="line">    y = x+x; <span class="comment">//y得到的值是10，而不是20。</span></span><br><span class="line">    z=x+<span class="built_in">this</span>.x; <span class="comment">//z得到的值是15。</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li>成员变量有默认值，但局部变量没有默认值，因此在使用局部变量之前，必须保证局部变量有具体的值，否则可能无法通过编译。例如：<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="keyword">class</span> <span class="title class_">A</span>&#123;</span><br><span class="line">   <span class="type">int</span> <span class="variable">x</span> <span class="operator">=</span> <span class="number">10</span> ,y;</span><br><span class="line">   <span class="keyword">void</span> <span class="title function_">f</span><span class="params">()</span>&#123;</span><br><span class="line">     <span class="type">int</span> m;     <span class="comment">//m没有默认值，但编译无错误</span></span><br><span class="line">     x = y+m;   <span class="comment">//无法通过编译，因为使用m之前未指定m的值</span></span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h6 id="实例方法和类方法"><a href="#实例方法和类方法" class="headerlink" title="实例方法和类方法"></a>实例方法和类方法</h6>定义：</li><li>方法声明时，方法类型前面不加关键字static修饰的是实例方法，加static关键字修饰的是类方法（静态方法）。例如：<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="type">float</span> <span class="title function_">min</span><span class="params">(<span class="type">float</span> x,<span class="type">float</span> y)</span>&#123;  <span class="comment">//实例方法</span></span><br><span class="line">      ...</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">speak</span><span class="params">(string s)</span>&#123;  <span class="comment">//类方法</span></span><br><span class="line">      ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>区别：</li><li>对象调用实例方法。只有该类创建对象后，类中的实例方法才分配入口地址，从而类方法可以被类创建的任何对象调用执行。而且创建第一个对象后，再创建对象时，不再分配入口地址。即方法的入口地址被所有的对象共享，当所有的对象都不存在时，方法的地址入口才被取消。实例方法中可以操作实例变量，也可以操作类变量。</li><li>类名调用类方法。对于类中的类方法，在该类被加载到内存时，就被分配了相应的入口地址，从而可以被类创建的任何对象调用执行，也可以直接通过类名调用，而且直到程序结束时入口地址才被取消。因为在类创建对象之前，成员变量还没有分配内存，所以类方法不可以操作实例变量。</li></ul><h4 id="需要注意的问题"><a href="#需要注意的问题" class="headerlink" title="需要注意的问题"></a>需要注意的问题</h4><p>在类体中，对成员变量的操作只能放在方法中，方法使用各种语句对成员变量和方法体中声明的局部变量进行操作。声明成员变量时可赋予初值，例如：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span>&#123;</span><br><span class="line">  <span class="type">int</span> <span class="variable">a</span> <span class="operator">=</span> <span class="number">3</span>;</span><br><span class="line">  <span class="type">float</span> <span class="variable">b</span> <span class="operator">=</span> <span class="number">1.32f</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但不可以这样做：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span>&#123;</span><br><span class="line">  <span class="type">int</span> a;</span><br><span class="line">  <span class="type">float</span> b;</span><br><span class="line">  a = <span class="number">3</span>;        <span class="comment">//非法，这是赋值语句（语句不是声明语句，只能出现在方法体中）   </span></span><br><span class="line">  b = <span class="number">1.32f</span>;   <span class="comment">//非法</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="访问权限"><a href="#访问权限" class="headerlink" title="访问权限"></a>访问权限</h3><p>每个库的用户(client programmer)必须能依赖自己使用的库，并知道一旦新版本的库推出，自己不需要改写代码。<br>为解决这个问题，Java推出了“访问权限”的概念，允许库创建者声明哪些是客户程序员可以是使用的，哪些是不可以使用的。<br>Java中为类中成员设置了4种访问权限，为类设置了2种访问权限 </p><h4 id="类中成员的访问权限"><a href="#类中成员的访问权限" class="headerlink" title="类中成员的访问权限"></a>类中成员的访问权限</h4><p>针对类内的每个成员的每个定义，Java访问权限 缺省，public,  protected和private都放在它们的前面—无论是数据成员还是方法。 </p><p><img src="https://s2.loli.net/2022/03/30/1gBUZTNsu2znq48.png" alt="20220330214402"></p><h4 id="类的访问权限"><a href="#类的访问权限" class="headerlink" title="类的访问权限"></a>类的访问权限</h4><p>在Java中，也可用访问权限控制着客户程序员是否能够创建属于这个类的一个对象。<br>声明一个类可使用的权限修饰符只有<strong>public</strong>和<strong>缺省</strong>两种，不能使用<strong>protected</strong>和<strong>private</strong>。</p><p>然而，我们同时还要注意到一些额外的限制：<br>(1) 每个编译单元（文件）都只能有一个public类。每个编译单元有一个公共接口的概念是由那个公共类表达出来的。根据自己的需要，它可拥有任意多个提供支撑的“友好”类。但若在一个编译单元里使用了多个public类，编译器就会向我们提示一条出错消息。<br>(2) public类的名字必须与包含了编译单元的那个文件的名字完全相符，甚至包括它的大小写形式。如果出现不符，就会报告一个编译期错误。<br>(3) 可能（但并常见）有一个编译单元根本没有任何公共类。此时，可按自己的意愿任意指定文件名。</p><h2 id="子类与继承"><a href="#子类与继承" class="headerlink" title="子类与继承"></a>子类与继承</h2><p>Java不支持多重继承，即子类只能有一个父类。</p><h3 id="supe关键字"><a href="#supe关键字" class="headerlink" title="supe关键字"></a>supe关键字</h3><h4 id="使用super调用父类的构造方法"><a href="#使用super调用父类的构造方法" class="headerlink" title="使用super调用父类的构造方法"></a>使用super调用父类的构造方法</h4><p>由于子类不继承父类的构造方法，因此子类在其构造方法中需使用super来调用父类的构造方法，<strong>而且super必须是子类构造方法中的头一条语句</strong>，即如果在子类的构造方法中，没有明显地写出super关键字来调用父类的某个构造方法，那么默认有：</p><blockquote><p>super();</p></blockquote><h4 id="用super操作被隐藏的成员变量和方法"><a href="#用super操作被隐藏的成员变量和方法" class="headerlink" title="用super操作被隐藏的成员变量和方法"></a>用super操作被隐藏的成员变量和方法</h4><p>子类一但隐藏了继承的成员变量，那么子类创建的对象就不再拥有该变量，该变量将归关键字super所拥有，同样，子类一但隐藏了继承的方法，那么子类创建的对象就不能调用被隐藏的方法，该方法由关键字super负责。<strong>因此，如果在子类中使用被子类隐藏的成员变量或方法，就需要使用关键字super</strong>。<br><em>需注意</em>：当super调用被隐藏的方法时，该方法中出现的成员变量是<strong>被子类隐藏的成员变量或继承的成员变量</strong>。（该方法中若出现子类隐藏的成员变量，被操作的是被子类隐藏的成员变量而不是子类重新声明的成员变量）</p>]]></content>
      
      
      <categories>
          
          <category> java </category>
          
      </categories>
      
      
        <tags>
            
            <tag> java零基础 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
